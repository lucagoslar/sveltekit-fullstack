version: '3.8'

services:
  web:
    build: .
    ports:
      - '3000:3000'
    env_file:
      - .env.production
#   networks:
#     - traefik
#   labels:
#     - 'traefik.enable=true'
#     - 'traefik.http.routers.sveltekitfullstack.rule=Host(`web.docker.localhost`)'
#     - 'traefik.http.routers.sveltekitfullstack.entrypoints=websecure'
#     - 'traefik.http.routers.sveltekitfullstack.tls.certresolver=letsencrypt'
#
# networks:
#   traefik:
#     external: true
